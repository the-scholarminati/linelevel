//attaching controllers to main until we find reason to create specific modules

angular.module('main').controller('createEventController',[
  function(){

  }
]);


//app.module('main').requires.push('createEvent');