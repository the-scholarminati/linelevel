//attaching controllers to main until we find reason to create specific modules

angular.module('main').controller('signUpController',[
  function(){
    
  }
]);

//app.module('main').requires.push('signup');